# متطلبات مراجعة المرحلة 0.1

## الغرض من الوثيقة

تحدد هذه الوثيقة المطلوب مراجعته قبل إدخال أي بيانات حقيقية إلى مشروع `dz-bet`. الهدف هو التأكد من صحة المنطق الرياضي والزمني والاختبارات، ثم إصلاح الأخطاء المحتملة قبل تنفيذ الباكتيست.

> **قاعدة إلزامية:** لا تُدخل بيانات حقيقية، ولا تُعلن أي إشارة، ولا تنتقل إلى المرحلة 1 قبل إغلاق مراجعة الكود وإصلاح ملاحظاتها.

## الوضع الحالي

المستودع في حالة `HOLD`. توجد نواة برمجية للمرحلة 0، وقد نجحت اختبارات الوحدات الحالية وعددها خمسة. لم تُثبت بعد صحة النموذج على بيانات حقيقية، ولم يُحسب ROI فعلي، ولم يُثبت وجود Confluence.

## الملفات المطلوب مراجعتها

### 1. الكود الأساسي

يجب فحص الوحدات الحالية أو إعادة تنظيمها إلى وحدات مستقلة عند الحاجة:

```text
src/dzbet/poisson.py
src/dzbet/roi.py
src/dzbet/steam.py
src/dzbet/confluence.py
src/dzbet/devils_advocate.py
src/dzbet/config.py
```

في البنية الحالية توجد الوظائف المناظرة داخل:

```text
src/dzbet/baseline.py
src/dzbet/signals.py
src/dzbet/gate.py
src/dzbet/data.py
src/dzbet/cli.py
```

لا يُشترط تغيير أسماء الملفات إذا ثبت أن الفصل الحالي واضح وقابل للتدقيق. أما إذا أدى التجميع إلى إخفاء حدود المسؤولية أو صعوبة الاختبار، فيجب فصل الوحدات.

### 2. الاختبارات

يجب مراجعة جميع الاختبارات:

```text
tests/test_*.py
```

ويجب تصنيفها بوضوح إلى:

| النوع | الغرض |
|---|---|
| Unit Test | اختبار دالة أو وحدة واحدة بمدخلات مضبوطة |
| Integration Test | اختبار تدفق كامل يبدأ من ملفات البيانات وينتهي بمخرج قابل للفحص |
| Statistical Test | اختبار خصائص النموذج والقياس دون خلطه بإثبات إشارة تجارية |

لا يكفي اختبار أن الدالة تعيد النتيجة التي صممها الاختبار نفسه. يجب إضافة حالات مستقلة تكشف الأخطاء، مثل التسرب الزمني، وخلط سعر ما بعد بداية المباراة، واختيار closing odds خاطئ.

### 3. الإعدادات وواجهة التشغيل

يجب مراجعة:

```text
pyproject.toml
src/dzbet/cli.py
```

ويجب التأكد من أن العتبات والافتراضات المهمة قابلة للتتبع من إعداد واحد، لا موزعة داخل الكود، وتشمل على الأقل:

- عتبة Steam Move.
- نافذة Steam Move الزمنية.
- الحد الأدنى لحجم العينة.
- اسم موسم Hold-out.
- تعريف closing odds.
- نافذة Confluence.
- مستوى الدلالة الإحصائية عند إضافة الاختبارات النصية.

## أسئلة التدقيق الإلزامية

### أ. نموذج Poisson

يجب الإجابة بوضوح عن الأسئلة التالية:

1. هل دالة الاحتمال مطبقة رياضيًا بصورة صحيحة؟
2. هل تُقدّر قوة الهجوم والدفاع وميزة الأرض بطريقة مستقرة؟
3. هل القيود اللازمة لتثبيت المعلمات مطبقة؟
4. هل يوجد انحياز بسبب الفرق الصاعدة والهابطة أو تغير أسماء الفرق؟
5. هل يكفي Poisson الخالص للمرحلة 0، أم يجب إضافة Dixon–Coles؟
6. إذا أُضيف Dixon–Coles، هل تقاس فائدته خارج العينة مقابل Poisson الخالص؟
7. هل احتمالات Home / Draw / Away مجموعها قريب من 1 بعد قص عدد الأهداف؟

### ب. ROI وClosing Odds

يجب التحقق من أن:

1. آخر timestamp مستخدم هو آخر سعر **قبل بداية المباراة**، وليس آخر سجل بعد بدايتها.
2. توجد مطابقة صحيحة بين المباراة والسوق والاختيار واسم شركة الأودز.
3. لا تُستخدم بيانات مستقبلية عند بناء القرار.
4. تُسجل لحظة القرار ولحظة closing line كل على حدة.
5. تُحسب العوائد على أساس odds عشرية وبصيغة واضحة.
6. يُقارن ROI مع baseline مناسب، ولا يُعرض Hit Rate وحده.
7. تُذكر العمولات أو هامش السوق أو تكاليف التنفيذ إن كانت متاحة.

### ج. Steam Move

يجب التحقق من:

1. أن نافذة الخمس دقائق معرفة في الإعدادات وليست رقمًا مخفيًا.
2. أن عتبة `0.10` معرفة في الإعدادات ومبررة كعتبة أولية، لا كحقيقة مثبتة.
3. أن التغير يُحسب داخل نفس bookmaker والسوق والاختيار.
4. أن التكرارات ومشاكل timestamp لا تنتج تحركات وهمية.
5. أن المقارنة بين Pinnacle أو Circa وBet365 أو William Hill تستخدم timestamps قابلة للمقارنة.
6. أن تعريف التأخر السعري يتضمن مرجعًا زمنيًا واضحًا، مثل أكثر من 30 ثانية.
7. أن عتبة Steam لا تُحسن باستخدام Hold-out.

### د. Devil’s Advocate

يجب إثبات أن الفحوص فعلية وليست شكلية. وتشمل على الأقل:

- منع استخدام Hold-out في اختيار العتبات.
- رفض closing odds غير المثبتة.
- رفض الإشارات غير المحاذاة زمنيًا.
- رفض العينات الصغيرة.
- كشف غياب السوق أو الاختيار أو timestamp.
- التنبيه عند وجود عدد كبير من الفرضيات أو التحسينات بعد الاطلاع على النتائج.
- تسجيل سبب رفض التقرير، لا الاكتفاء بقيمة منطقية عامة.

### هـ. الحد الأدنى لحجم العينة

القيمة الحالية هي `100` رهان. يجب توثيق:

1. لماذا اختير هذا الرقم.
2. هل هو حد هندسي أم إحصائي.
3. ما حجم العينة اللازم لتقدير ROI بفاصل ثقة مفيد.
4. هل يُطبق الحد على إجمالي الرهانات أم على كل موسم أو كل نوع إشارة.
5. ماذا يحدث إذا كان عدد الفرص أقل من الحد.

إلى أن يُبرر الرقم إحصائيًا، يجب اعتباره **حاجزًا أوليًا محافظًا** لا معيار نجاح نهائيًا.

### و. الفصل الزمني

يجب توثيق طريقة تحديد:

```text
train → validation/test → hold-out
```

المطلوب هو:

- فصل زمني لا عشوائي.
- تحديد الموسم الأخير كـ Hold-out قبل رؤية نتائجه.
- عدم استخدام Hold-out لاختيار النموذج أو العتبات.
- توثيق تواريخ بداية ونهاية كل جزء.
- منع odds أو نصوص أو features ذات timestamp لاحق من دخول القرار السابق.

## المطلوب تسليمه في نهاية مراجعة 0.1

يجب أن تنتج المراجعة المستندات والنتائج التالية:

### 1. تقرير أخطاء الكود

يتضمن لكل ملاحظة:

| الحقل | الوصف |
|---|---|
| المعرّف | رقم ثابت للملاحظة |
| الملف والسطر | موضع المشكلة |
| الشدة | حرجة، عالية، متوسطة، منخفضة |
| الوصف | ما الذي يحدث |
| الأثر | كيف يؤثر على النتيجة |
| الإصلاح المقترح | التغيير المطلوب |
| اختبار التحقق | كيف نثبت أن الإصلاح صحيح |

### 2. تقرير المخاطر الإحصائية

يجب أن يغطي:

- خطر التسرب الزمني.
- خطر اختيار العتبات بعد رؤية النتائج.
- خطر survivorship أو اختيار الفرق والمباريات.
- عدم استقرار ROI بسبب قلة الرهانات.
- الفرق بين Hit Rate وROI.
- أثر هامش شركة الأودز.
- تعدد الاختبارات في OSINT.
- الحاجة إلى فواصل ثقة واختبارات خارج العينة.

### 3. اختبارات إضافية

يجب إضافة اختبارات تكامل حقيقية، منها:

- تشغيل تدفق validation على ملفات CSV صحيحة.
- رفض مباراة يكون closing odds لها بعد وقت البداية.
- التأكد من عدم دخول Hold-out في التدريب.
- التحقق من اختيار آخر سعر صالح قبل المباراة.
- التحقق من أن تقرير HOLD يشرح أسباب الرفض.

### 4. سجل قرار المراجعة

النهاية يجب أن تكون واحدة من:

| القرار | المعنى |
|---|---|
| `READY_FOR_FIXES` | المراجعة اكتملت، وتوجد ملاحظات يجب إصلاحها قبل البيانات |
| `BLOCKED` | توجد مشكلة حرجة تمنع الاستمرار |
| `CODE_REVIEW_PASSED` | لا توجد عيوب حرجة، ويمكن الانتقال إلى مرحلة الإصلاح والاختبارات الإضافية |

هذا القرار لا يعني السماح بإدخال البيانات. إدخال البيانات يأتي فقط بعد إغلاق الإصلاحات والاختبارات.

## ما يأتي بعد إغلاق 0.1

بعد اعتماد مراجعة الكود وإصلاحاتها، تكون الخطوات بالترتيب:

1. **المرحلة 0.2:** إصلاح العيوب وإضافة اختبارات Integration.
2. **اختيار الدوري:** Eredivisie أو Championship أو دوري يحدده صاحب المشروع.
3. **المرحلة 0.3:** إدخال بيانات حقيقية موثقة، دون لمس Hold-out.
4. **المرحلة 0.4:** تشغيل `validate` ثم `backtest`.
5. **المرحلة 0.5:** اتخاذ قرار PASS أو HOLD أو FAIL.

## القرار الحالي

```text
CURRENT STATUS: HOLD
NEXT ALLOWED ACTION: CODE REVIEW 0.1
PROHIBITED FOR NOW: REAL DATA, PRODUCTION LINES, PHASE 1
```

## المراجع

لا يعتمد هذا المستند على بيانات خارجية. وهو يحول متطلبات مستند الخطة المرفق إلى قائمة تنفيذ ومراجعة قابلة للتتبع داخل المستودع.
