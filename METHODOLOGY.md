# المنهجية

## الغرض

تحدد هذه الوثيقة قرارات المرحلة 0 قبل إدخال البيانات الحقيقية. لا يجوز تغيير هذه القرارات بعد رؤية نتائج الـhold-out. أي تغيير لاحق يُسجل كإصدار منهجي جديد ويُعامل كإعادة تجربة مستقلة.

## تعريف الموسم

المشروع يستهدف دوريًا أوروبيًا بنظام موسم رياضي، وليس سنة تقويمية. الموسم `YYYY-YYYY` يبدأ في 1 يوليو من `YYYY` وينتهي في 30 يونيو من `YYYY+1`. يجب أن تكون قيمة `season` في كل ملف متسقة مع هذا التعريف، ويُرفض السجل إذا لم يتوافق تاريخ المباراة مع موسمها المعلن.

سيُستخدم مصدر بيانات موثق لكل من نتائج المباريات ووقت kickoff والأودز. اسم المصدر وإصدار التنزيل وتاريخ الحصول عليه ستُحفظ في تقرير البيانات قبل تشغيل الباكتيست.

## تقسيم البيانات

التقسيم زمني وعلى مستوى الموسم، وليس عشوائيًا:

| الجزء | القاعدة المحددة |
|---|---|
| Train | 2019-20، 2020-21، 2021-22، 2022-23 |
| Test | 2023-24، 2024-25 |
| Hold-out | 2025-26، يحدد قبل أي ضبط ولا يُستخدم في الاختيار |

تم اعتماد التقسيم النهائي **4 Train + 2 Test + 1 Hold-out** لأن مجموعة المرحلة 0.3 تتكون من سبعة مواسم كاملة. هذا القرار ثابت قبل إدخال البيانات.

خيار `--holdout-season` يقبل موسمًا واحدًا فقط في المرحلة 0، ويرفض أي محاولة لوجوده في Train أو Test. لا يسمح هذا الخيار بقائمة مواسم حتى لا تختلط قواعد التجربة.

## baseline المرجعي

تُقارن النتائج بثلاثة baselines محددة مسبقًا:

1. **Base Rate:** احتمالات 1X2 المستخرجة من تكرار النتائج في Train، مع smoothing بسيط عند الحاجة.
2. **Odds-Implied Probabilities:** الاحتمالات الناتجة من معكوس odds ثم تطبيعها لإزالة overround، باستخدام quote قبل kickoff وفق نفس تعريف closing odds.
3. **Poisson الخالص:** نموذج الأهداف الحالي دون Dixon–Coles.

لا تُعد استراتيجية Poisson إشارة قابلة للاستغلال إلا إذا تفوقت خارج العينة على Base Rate وOdds-Implied baseline بمقياس احتمالي و/أو ROI بعد تكاليف التنفيذ.

## نموذج Poisson

يُستخدم Poisson مستقل للأهداف المنزلية والخارجية مع معلمات هجوم ودفاع وميزة أرض وقيود sum-to-zero. لا يُستخدم Dixon–Coles في النسخة الأولى. يمكن إدخاله لاحقًا فقط في مقارنة A/B خارج العينة، مع إبقاء Hold-out معزولًا.

تُحسب احتمالات 1X2 من مصفوفة الأهداف حتى حد أقصى 10 أهداف ثم تُطبّع. يجب قياس الكتلة المفقودة قبل التطبيع. إذا تجاوزت 1% في بيانات التشغيل، يُرفع الحد إلى 15 وتُعاد المقارنة خارج العينة.

## الفرق الصاعدة والهابطة

لا يُسمح بتسريب نتائج الموسم الجديد إلى تقديراته. الفريق الصاعد الذي لا يملك سجلًا في Train يُعطى معلمات ابتدائية محايدة (هجوم ودفاع يساويان متوسط الدوري) مع ميزة الأرض العامة. تُسجل هذه الحالات، وتُحلل منفصلة. لا تُستخدم أسعار الفريق الصاعد لإثبات إشارة دون حجم كافٍ.

## اختلاف عدد المباريات والتأجيلات

Poisson يعتمد على أهداف كل مباراة ولا يفترض تساوي عدد المباريات. اختلاف عدد المباريات يؤثر في دقة المعلمات فقط، لذلك تُسجل عدد مباريات كل فريق، ويُمنع استخدام ميزات ترتيبية غير مطبّعة دون معالجة. تُستخدم تواريخ kickoff الفعلية في الترتيب الزمني، ولا تُستخدم أرقام الجولات كبديل.

## Closing odds وتوقيت القرار

closing odds هي آخر quote صالح قبل kickoff لنفس المباراة والسوق والاختيار وbookmaker. أي quote بعد kickoff يُستبعد. تُحفظ لحظة quote ولحظة kickoff، ويجب أن يكون القرار قابلًا للتنفيذ قبل kickoff. لا يُسمح باستخدام معلومات أو منشورات بعد وقت القرار.

## تكلفة التنفيذ — دين تقني

قبل إعلان أي إشارة يجب تحديد واحتساب:

- العمولة: `TBD`.
- الانزلاق السعري: `TBD`.
- هامش السوق/overround: يُقاس من odds المتاحة.
- حدود السيولة والتنفيذ: `TBD`.

ROI الخام لا يكفي للقبول. يجب إصدار ROI صافٍ بعد تثبيت هذه القيم، مع فواصل ثقة وحساسية للانزلاق.

## حدود الاختبار الإحصائي

الحد الأولي `min_bets=100` حاجز جودة وليس إثبات قوة. تُعرض فواصل ثقة للـROI، ويُفصل Hit Rate عن ROI. اختبارات OSINT المتعددة تحتاج تصحيحًا مناسبًا قبل اعتبار `p < 0.05` دليلًا.

## قواعد عدم التغيير

يُثبت اسم الدوري، تعريف الموسم، التقسيم، baseline، العتبات، وسياسة التكاليف في تقرير قبل إدخال البيانات. أي تعديل بعد ظهور نتيجة يُعتبر تجربة جديدة ولا يحق له استخدام نفس Hold-out كأنه لم يُرَ.

## بوابة الانتقال

تبقى الحالة `HOLD` حتى تُراجع هذه المنهجية، وتنجح اختبارات التسرب، وتُحدد قيم التكلفة، ثم تُدخل بيانات حقيقية موثقة. لا تنتقل المنهجية وحدها إلى المرحلة 0.3 ولا تثبت وجود إشارة.

## Revision 3 — 2026-09-12

يستخدم خط البيانات أعمدة `PSCH` و`PSCD` و`PSCA` بوصفها Pinnacle Closing، مع استخدام `B365CH` و`B365CD` و`B365CA` كبديل إذا غابت أعمدة Pinnacle. لا تُستخدم أعمدة `AvgCH` و`AvgCD` و`AvgCA` لتعريف closing odds.

لا يوفر المصدر timestamp دقيقًا للquote. لذلك تُقبل دلالة `Closing` كما يعرّفها المصدر، لكن لا يجوز الادعاء بإثبات قاعدة `timestamp < kickoff` حرفيًا. تُحفظ قيمة timestamp كـ`NaT` بدل اختلاق وقت.

لا يدعم Understat دوري E1، ولم تُثبت تغطية StatsBomb له. تستخدم النسخة الحالية أهداف `FTHG` و`FTAG` كميزات أولية لنموذج Poisson. هذا قيد معلن يضعف النموذج ولا يثبت وجود إشارة.

أظهر تدقيق OpenFootball أن 77 زوجًا في موسم 2019-20 تحمل تواريخ مزاحة 366 يومًا، بينما تتطابق أزواج الفرق بعد التطبيع. تُستخدم OpenFootball للتحقق العشوائي بعد توثيق هذا العيب، ولا تُستخدم لتعديل تواريخ أو نتائج football-data الأساسية.
