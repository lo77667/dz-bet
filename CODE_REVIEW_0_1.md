# تقرير مراجعة الكود 0.1

## القرار

```text
DECISION: CODE_REVIEW_PASSED
NEXT: إصلاحات إضافية وتحسين اختبارات التكامل قبل إدخال البيانات الحقيقية
STATUS: HOLD بالنسبة للبيانات والمرحلة 1
```

تمت مراجعة الوحدات الأساسية وإعادة فصلها إلى ملفات مستقلة. لم تُدخل بيانات حقيقية ولم يُشغّل باكتيست تجاري.

## الملاحظات المكتشفة والمعالجة

| المعرّف | الشدة | الملاحظة | المعالجة | اختبار التحقق |
|---|---|---|---|---|
| CR-001 | عالية | كان اختيار آخر odds يتم عبر timestamp فقط، ما يسمح نظريًا بسعر بعد kickoff | أضيف ربط بوقت المباراة واستبعاد كل quote بعد `date` قبل اختيار closing quote | `test_post_kickoff_quotes_are_excluded` |
| CR-002 | متوسطة | مصفوفة Poisson المقطوعة عند 10 أهداف لا يكون مجموعها 1 تمامًا | أضيف تطبيع لاحتمالات Home/Draw/Away بعد القص | `test_poisson_probabilities_sum_to_one` |
| CR-003 | متوسطة | العتبات كانت موزعة بين الدوال | أضيفت `Phase0Config`، وتُمرر العتبات إلى CLI والكاشف | اختبارات الكاشف الحالية |
| CR-004 | متوسطة | كانت اختبارات الوحدات موجودة دون اختبار تدفق زمني كامل | أضيفت اختبارات تكامل مستقلة للـ closing odds وPoisson وترتيب timestamps | `tests/test_phase0_integration.py` |

## نتائج تدقيق الوحدات

### Poisson

النموذج الحالي Poisson مستقل للأهداف المنزلية والخارجية مع قوة هجوم ودفاع وميزة أرض. القيود sum-to-zero تمنع عدم تعريف مقياس الهجوم والدفاع. لا يستخدم Dixon–Coles بعد. قرار استخدام Dixon–Coles يجب أن يكون مقارنة خارج العينة، لا افتراضًا مسبقًا.

### ROI وclosing odds

يُحسب ROI لكل رهان موجب edge، وتُحسب العوائد من odds عشرية. يتم الآن اختيار آخر quote قبل kickoff لكل مباراة وسوق واختيار. لا تزال تكلفة التنفيذ، عمولة الرهان، وهوامش السوق غير ممثلة لأنها تتطلب تعريفًا ومصدرًا فعليًا قبل إدخال البيانات.

### Steam Move

الافتراضات الحالية هي `0.10` و`300` ثانية. هذه عتبات أولية قابلة للضبط وليست نتائج مثبتة. لا يجوز تحسينها على hold-out. الكاشف يعمل داخل نفس bookmaker والسوق والاختيار، ويرتب timestamps ويتجاهل الفواصل الزمنية السالبة.

### Devil’s Advocate

الفحوص تمنع استخدام hold-out في الضبط، وتتحقق من إثبات closing odds، ومحاذاة timestamps، وحد حجم العينة. ما زال يلزم في مرحلة لاحقة ربط هذه الفحوص بمصدر بيانات موثق وتسجيل metadata كامل لكل تقرير.

### حجم العينة والفصل الزمني

`min_bets=100` حاجز أولي محافظ، وليس ضمانًا إحصائيًا لقوة ROI. فصل hold-out حاليًا صريح عبر `--holdout-season`. قبل البيانات الحقيقية يجب توثيق تواريخ kickoff وموسم الاختبار، وإضافة فواصل ثقة للـ ROI.

## نتيجة التشغيل

الأمر المطلوب:

```bash
pytest -v
```

النتيجة:

```text
8 passed in 3.47s
```

الاختبارات تشمل خمسة اختبارات وحدات وثلاثة اختبارات تكامل.

## القيود المتبقية

لا يثبت هذا التقرير وجود إشارة. لا توجد بعد بيانات حقيقية أو ROI أو p-value أو نتيجة Confluence. الخطوة التالية المسموحة هي إغلاق أي تحسينات متبقية في مرحلة 0.2، ثم اختيار دوري ومصدر بيانات قبل تشغيل الباكتيست.
